export default class User {
    constructor(username,password,nome, email) {
        this.username = username;
        this.password = password;
        this.nome = nome;
        this.email = email;
    }
}